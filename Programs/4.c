#include <stdio.h>
#include <stdlib.h>

int main(int argv, char *argc[])
{
    int i, o, max1, max2 = 0;
    char number[] = "";

    for (i; i >= 999; i++){
        for (o; o >= 999; o++)
            itoa ();
    }
    return 0;
}
